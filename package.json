{
  "name": "template",
  "license": "MIT",
  "workspaces": ["core", "web", "api"],
  "scripts": {
    "branch": "git checkout main && git pull && git branch -D $(whoami) ; git checkout -b $(whoami)",
    "b": "bun branch",
    "dev": "bun --cwd api dev & bun --cwd web dev",
    "start": "bun --cwd api prod",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "wip": "git add . && git commit -m 'wip' && git push",
    "ship": "bun --cwd web build && rm -rf docs/* && cp -r web/res/* docs/ && git add . && git commit -m 'ship' && git push",
    "s": "bun ship"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@template/core": "0.1.2",
    "bun-types": "^1.3.6"
  }
}
